{
  "name": "utm-builder",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node -e \"const fs=require('fs'); const file='dist/client/wrangler.json'; if(fs.existsSync(file)){ const data=JSON.parse(fs.readFileSync(file)); delete data.kv_namespaces; fs.writeFileSync(file, JSON.stringify(data)); }\"",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.2",
    "tailwindcss": "^4.3.0"
  }
}
